{
  "version": 2,
  "projects": [
    {
      "src": "apps/web",
      "buildCommand": "npm run build",
      "outputDirectory": "apps/web/.next",
      "framework": "nextjs"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "apps/web/$1" }
  ]
}